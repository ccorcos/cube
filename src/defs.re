/* size of the cube */
let size = 200.;

/* minimum distance before drawing a new point on a path */
let minDelta = 5.;

/* size basis of the svg block */
let viewBox = 1000;

/* color options */
let colorOptions = ["black", "white", "red", "orange", "yellow", "green", "blue", "purple"];

/* stoke size options */
let widthOptions = [1, 2, 4, 8, 16, 32, 64];
